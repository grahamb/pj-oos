<!DOCTYPE html>
<html>
  <head>
    <title>PJ 2015 Program {{title}}</title>
    <!--[if (gte IE 6)&(lte IE 8)]>
      <script type="text/javascript" src="vendor/selectivizr-min.js"></script>
    <![endif]-->
    <!--[if lt IE 9]>
        <script src="vendor/html5shiv.min.js"></script>
        <script src="vendor/es5-shim.min.js"></script>
        <script src="vendor/es5-sham.min.js"></script>
    <![endif]-->
    <link href="//maxcdn.bootstrapcdn.com/font-awesome/4.2.0/css/font-awesome.min.css" rel="stylesheet">
    <link rel='stylesheet' href='/css/style.css' />
    <script src="//code.jquery.com/jquery-1.11.0.min.js"></script>
  </head>
  <body>
    {{> navbar}}

    {{#unless production}}
      <div class="flash-alert" style="text-align: center">
        Development Mode
      </div>
    {{/unless}}
    {{#if messages}}
    <section class="outer messages">
      {{#if messages.error}}
        {{#each messages.error}}
          <div class="flash-error">
            <span>{{this}}</span>
          </div>
        {{/each}}
      {{/if}}

      {{#if messages.success}}
        {{#each messages.success}}
          <div class="flash-success">
            <span>{{this}}</span>
          </div>
        {{/each}}
      {{/if}}
    </section>
    {{/if}}

    {{{body}}}
    <script>
        $(document).ready(function() {
          $('#js-navigation-menu').removeClass("show");

          $('#js-mobile-menu').on('click', function(e) {
            e.preventDefault();
            $('#js-navigation-menu').slideToggle(function(){
              if($('#js-navigation-menu').is(':hidden')) {
                $('#js-navigation-menu').removeAttr('style');
              }
            });
          });
        });
    </script>

    {{#if production}}

    <script>
      (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
      (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
      m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
      })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

      ga('create', 'UA-58647110-1', 'auto');
      ga('send', 'pageview');

    </script>

    {{/if}}
  </body>
</html>
